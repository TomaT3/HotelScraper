services:
  hotelscraper:
    build: .
    container_name: hotel-price-tracker
    ports:
      - "8080:8000"
    volumes:
      - ./data:/app/data
    environment:
      - RAPIDAPI_KEY=${RAPIDAPI_KEY}
      - Scraper__DatabaseUrl=Data Source=data/hotel_prices.db
      - Scraper__DatesPerRun=${DATES_PER_RUN:-15}
      - Scraper__FetchHour=${FETCH_HOUR:-3}
      - Scraper__SearchCities=${SEARCH_CITIES:-Stuttgart}
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
